originalView
       the jboss seam original view template

newView
the different to originalView:
       1.the label value get from the resource file.
       2.the pages generated put into the different directory      